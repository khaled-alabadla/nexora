; Xdebug is present in the image but inert unless XDEBUG_MODE is set to something
; other than "off" (see docker-compose.yml). This keeps normal dev/CI fast.
xdebug.mode = ${XDEBUG_MODE}
xdebug.client_host = host.docker.internal
xdebug.client_port = 9003
xdebug.start_with_request = trigger
